DISPLAY=:99 npx playwright test ${{ matrix.group.file }} \
  --project="Google Chrome" \
  --workers=1 \
  --headed \
  --reporter=json:test-results/report.json \
  2>&1 | tee test_output.txt || EXIT_CODE=$?
