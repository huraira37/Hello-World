set -o pipefail && EXIT_CODE=0 && DISPLAY=:99 npx playwright test ${{ matrix.group.file }} --project="Google Chrome" --workers=1 --headed --reporter=list,json=./test-results/report.json 2>&1 | tee test_output.txt || EXIT_CODE=$? && echo "Test exit code: $EXIT_CODE" && echo "exit-code=$EXIT_CODE" >> $GITHUB_OUTPUT && exit $EXIT_CODE
