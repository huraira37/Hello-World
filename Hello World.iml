this is my code:

  public async validatedUnderwritingPausePopup() {
    const page = this.page;
    page.waitForLoadState('domcontentloaded');
    await new Promise(resolve => setTimeout(resolve, 5000));
    await expect(this.underwritingPause).toHaveText("Underwriting Pause", { timeout: 30000 })
    this.logger.log("Validated the Under Writing Pause Pop-up");
  }


* but receiving below error:





Error: Timed out 30000ms waiting for expect(locator).toHaveText(expected)

Locator: locator('//span[text()=\'Underwriting Pause\']')
Expected string: "Underwriting Pause"
Received: <element(s) not found>
Call log:
  - expect.toHaveText with timeout 30000ms
  - waiting for locator('//span[text()=\'Underwriting Pause\']')


  120 |     page.waitForLoadState('domcontentloaded');
  121 |     await new Promise(resolve => setTimeout(resolve, 5000));
> 122 |     await expect(this.underwritingPause).toHaveText("Underwriting Pause", { timeout: 30000 })

give me best solituion:
 
