This is my code:

public async searchCustomerExtranet(phoneNumber: string) {

    const page = this.page;
    this.logger.log("Phone number is " + phoneNumber);
    await this.phoneNumber.fill(phoneNumber);
    this.logger.log("Entered the phone number");
    await this.runSearchBtn.click();
    this.logger.log("Clicked on search button");
    page.waitForLoadState('domcontentloaded');
    await new Promise(resolve => setTimeout(resolve, 5000));
    await expect(page).toHaveTitle("Customer Service Workbench");
    await new Promise(resolve => setTimeout(resolve, 5000));
    await expect(this.cswPageHeading).toContainText("Customer Service Workbench");
    this.logger.log("Customer Service page is appeared");
  }




*But getting below error:

Error: Timed out 5000ms waiting for expect(locator).toHaveTitle(expected)

    Locator: locator(':root')
    Expected string: "Customer Service Workbench"
    Received string: ""
    Call log:
      - expect.toHaveTitle with timeout 5000ms
      - waiting for locator(':root')


       at ..\page-objects\cfAuto-objects\searchCustomer-page.ts:66

      64 |     page.waitForLoadState('domcontentloaded');
      65 |     await new Promise(resolve => setTimeout(resolve, 5000));
    > 66 |     await expect(page).toHaveTitle("Customer Service Workbench");
         |                        ^
      67 |     await new Promise(resolve => setTimeout(resolve, 5000));
      68 |     await expect(this.cswPageHeading).toContainText("Customer Service Workbench");
      69 |     this.logger.log("Customer Service page is appeared");

Give me best solution and update the code
