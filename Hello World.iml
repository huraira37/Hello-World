import { merge } from 'monocart-reporter';
import { globSync } from 'glob';
import path from 'path';

const run = async () => {
  try {
    // ✅ Find only valid index.json files from report folders
    const files = globSync('../all-artifacts/Monocart-*/index.json');

    console.log(`🧩 Found ${files.length} Monocart index.json file(s)`);
    files.forEach(file => console.log(' → ' + file));

    if (files.length === 0) {
      throw new Error('No valid index.json files found to merge.');
    }

    // ✅ Merge these JSON files into one
    await merge({
      files,
      output: '../merged-report'
    });

    console.log('✅ Merged Monocart report created at ../merged-report');
  } catch (error) {
    console.error('❌ Failed to merge Monocart reports:', error.message);
    process.exit(1);
  }
};

run();
