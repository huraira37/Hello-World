This is my code:

  public async validateOperatorOptions_cf1(operatorTypeValidation: string) {
    const page = this.page;
    const dropdown_Disabled = await this.restrictedOperator_cf1.locator("//option[text()='" + operatorTypeValidation + "']").isDisabled();
    await new Promise(resolve => setTimeout(resolve, 20000));
    expect(dropdown_Disabled).toBe(true);

  }


* I got this error:

Error: expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false

  61 |     const dropdown_Disabled = await this.restrictedOperator_cf1.locator("//option[text()='" + operatorTypeValidation + "']").isDisabled();
  62 |     await new Promise(resolve => setTimeout(resolve, 20000));
> 63 |     expect(dropdown_Disabled).toBe(true);
     |                               ^

Can you update my code with better assertion?
